<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.multicampus.foodiefair.dao.ICommentDAO">
    <insert id="commentInsert">
        INSERT INTO Comment (userId, reviewId, commentContent) VALUES (22, 22, 'commentContent 22');
    </insert>

    <delete id="commentDelete">
        DELETE FROM Comment WHERE commentId=21;
    </delete>

    <select id="commentRead" resultType="com.multicampus.foodiefair.dto.CommentDTO">
        SELECT reviewId, userId, commentContent FROM Comment WHERE reviewId=#{reviewId} order by commentDate asc;
    </select>
</mapper>
