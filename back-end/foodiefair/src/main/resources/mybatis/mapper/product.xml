<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.multicampus.foodiefair.dao.IProductDAO">
    <select id="findProductId" resultType="String">
        SELECT productId FROM product WHERE productName=#{productName};
    </select>

    <select id="productInfo" resultType="com.multicampus.foodiefair.dto.ProductDTO">
        SELECT productId, productName, fixedTag, productImg, productPrice, productReviews, productSaved, productFestival FROM product WHERE productId=#{productId};
    </select>
</mapper>
